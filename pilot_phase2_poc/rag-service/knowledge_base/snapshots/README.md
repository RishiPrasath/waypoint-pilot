# Knowledge Base Snapshots

This folder stores immutable source snapshot records.

`RAG-DT012` creates the first-pass manifest only. Full raw HTML/PDF snapshot
storage is deferred until acquisition automation, licensing review, and storage
rules are approved.

Current manifest:

```text
first-pass-snapshot-manifest.md
```
