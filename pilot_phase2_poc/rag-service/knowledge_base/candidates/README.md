# Knowledge Base Candidates

This folder stores cleaned markdown candidates produced from reviewed source
snapshots.

Candidates are not runtime retrieval material by default. Runtime ingestion may
use only sources that later move through review into `knowledge_base/canonical/`
with an approved registry row.

Current first-pass candidates:

```text
first-pass/
```
